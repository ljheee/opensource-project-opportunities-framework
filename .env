# 复制此文件为 .env 并按需修改
# cp .env.example .env

# LLM CLI 工具调用命令（含所有必要参数，末尾不加 prompt）
# 运行时脚本会在此命令后追加 "$PROMPT" 参数
#
# 示例：
#   claude --dangerously-skip-permissions  （默认，Claude Code CLI）
#   gemini --yolo                          （Gemini CLI）
#   aider --yes --no-git --message         （Aider）
# CLI_TOOL="mc --code --dangerously-skip-permissions"
CLI_TOOL="agent --force"
GITHUB_TOKEN="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"